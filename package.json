{
  "name": "mmd2uni",
  "version": "0.1.0",
  "description": "将 mermaid 图表转换为终端 Unicode 字符图（中文双宽对齐）",
  "type": "module",
  "bin": { "mmd2uni": "dist/main.js" },
  "files": ["dist"],
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc"
  },
  "dependencies": {
    "beautiful-mermaid": "^1.1.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.5.0",
    "vitest": "^3.0.0"
  }
}
